\chapter{Ensayos y Resultados}
\label{chap:ensayos_resultados}

En este capítulo se presentan las pruebas realizadas para validar que la infraestructura desplegada en el Capítulo~\ref{chap:diseno_implementacion} satisface los objetivos funcionales, de seguridad, de rendimiento y de continuidad operativa definidos en la Sección~\ref{sec:objetivos_alcance}. Las verificaciones se organizan por capas en orden ascendente: desde la confirmación del despliegue de cada servicio individual, pasando por la integración entre componentes, el control de acceso y el flujo funcional completo, hasta las métricas de consumo de recursos y la restauración de respaldos. Cada ensayo contrasta el comportamiento observado en producción contra el diseño previsto, y el capítulo cierra con una matriz de trazabilidad que vincula los requisitos formulados con la evidencia obtenida.

\section{Metodología de Validación}
\label{sec:metodologia_validacion}

El enfoque de validación se centra en verificar el comportamiento de los componentes desplegados, sus integraciones y el sistema en su conjunto bajo condiciones controladas de producción. Las verificaciones se organizan en cinco capas progresivas:

\begin{itemize}
    \item Capa de servicio: verificación de que cada contenedor inicia, supera su healthcheck y mantiene disponibilidad en el tiempo.
    \item Capa de integración: verificación de que los canales de comunicación entre servicios retornan respuestas exitosas y no registran errores en producción (Documenso con PostgreSQL, Browserless, MinIO y el relay SMTP de Brevo).
    \item Capa de sistema: ejecución del flujo de firma completo de extremo a extremo en producción, desde la carga del documento hasta la descarga del PDF sellado.
    \item Capa de rendimiento: observación del consumo de CPU y RAM durante operación normal y bajo carga controlada.
    \item Capa de continuidad: verificación de que los respaldos automáticos se almacenan correctamente y que un volcado puede restaurarse sin pérdida de datos.
\end{itemize}

Las herramientas empleadas para la recolección de evidencia fueron: la interfaz de Coolify (logs de contenedores y estado de servicios), las herramientas de desarrollo del navegador (trazas HTTP), Adobe Reader (validación de firma en PDF), Gmail (headers técnicos de autenticación SMTP) y Netdata. Este último se desplegó como un proyecto adicional dentro de Coolify para monitorear el rendimiento y los recursos consumidos por los contenedores del despliegue.

\section{Validación de Despliegue de Servicios}
\label{sec:validacion_despliegue}

Esta sección verifica que la capa de servicio descrita en la Sección~\ref{sec:metodologia_validacion} es operativa: cada contenedor del stack alcanzó el estado \texttt{Healthy} y superó las pruebas de disponibilidad configuradas en el Capítulo~\ref{chap:diseno_implementacion}. La validación se presenta en dos niveles complementarios: la verificación de los healthchecks a nivel de infraestructura desde la interfaz de Coolify y la consulta del endpoint de diagnóstico interno de Documenso.

\subsection{Estado de los contenedores en Coolify}
\label{subsec:validacion_healthcheck}

El archivo de composición del despliegue, presentado en la Sección~\ref{sec:configuracion_servicios} del Capítulo~\ref{chap:diseno_implementacion}, define una directiva \texttt{healthcheck} para cada servicio del stack. El panel de Coolify registra los servicios \texttt{documenso}, \texttt{database} y \texttt{browserless} con estado \texttt{Healthy} (Figura~\ref{fig:coolify_healthy_main}), lo que confirma que cada uno superó su healthcheck. La Figura~\ref{fig:coolify_healthy_socat} registra el estado \texttt{Healthy} para el servicio \texttt{socat}, desplegado en un proyecto de Coolify independiente.

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{figuras/capitulo4/4.2/documenso_browserless_database_healthyOnCoolify.png}
    \caption[Panel de Coolify: servicios principales en estado Healthy]{Panel de Coolify mostrando los servicios \texttt{documenso}, \texttt{database} y \texttt{browserless} con la etiqueta \texttt{Healthy}, lo que confirma que cada uno superó su healthcheck configurado.}
    \label{fig:coolify_healthy_main}
\end{figure}

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{figuras/capitulo4/4.2/socat_healthyOnCoolify.png}
    \caption[Panel de Coolify: servicio Socat en estado Healthy]{Panel de Coolify mostrando el servicio \texttt{socat} con la etiqueta \texttt{Healthy}, lo que confirma que el proxy TCP hacia MinIO está activo y acepta conexiones en el puerto 9000.}
    \label{fig:coolify_healthy_socat}
\end{figure}

\FloatBarrier

El estado \texttt{Healthy} de los cuatro servicios confirma que el stack completo superó sus pruebas de disponibilidad. Dado que \texttt{documenso} declara \texttt{depends\_on} sobre \texttt{database} con la condición \texttt{service\_healthy}, su propio estado \texttt{Healthy} implica además que PostgreSQL completó sus verificaciones antes de que la aplicación iniciara.

\subsection{Verificación del estado interno de la aplicación}
\label{subsec:validacion_api_health}

Documenso expone además el endpoint \texttt{/api/health}, que extiende la verificación de infraestructura hacia los subsistemas internos de la aplicación. A diferencia del healthcheck de Docker —que únicamente comprueba accesibilidad de red—, este endpoint reporta el estado de la conectividad con la base de datos y la disponibilidad del certificado de firma.

El Código~\ref{lst:api_health_response} muestra la respuesta obtenida al consultar \nolinkurl{https://documentos.facet.unt.edu.ar/api/health}.

\begin{lstlisting}[caption={Respuesta del endpoint \texttt{/api/health} de Documenso}, label={lst:api_health_response}]
{
  "status": "ok",
  "timestamp": "2026-04-12T15:01:14.367Z",
  "checks": {
    "database":    { "status": "ok" },
    "certificate": { "status": "ok" }
  }
}
\end{lstlisting}

Los tres campos con valor \texttt{"ok"} confirman que la aplicación está operativa, que Documenso estableció conectividad con PostgreSQL y que el certificado de firma institucional está cargado y disponible.

\FloatBarrier

\section{Verificación de la Comunicación entre Servicios}
\label{sec:verificacion_comunicacion}

Esta sección valida que los canales de comunicación entre los componentes del despliegue operan según el diseño descrito en el Capítulo~\ref{chap:diseno_implementacion}. Se presentan tres verificaciones: la conectividad con el almacenamiento de objetos MinIO a través del proxy Socat, la comunicación con Browserless para el renderizado del certificado de firma y la entregabilidad del correo transaccional cursado a través del relay de Brevo.

\subsection{Conectividad con el almacenamiento de objetos (MinIO)}
\label{subsec:validacion_minio}

Los logs HTTP del servidor MinIO registran los dos flujos de acceso al bucket \texttt{documenso-prod}: el directo, originado desde el navegador del usuario, y el indirecto, originado desde el backend de la aplicación.

La Figura~\ref{fig:minio_log_flujo_directo} muestra una operación \texttt{PUT 200 OK} cuyo campo \texttt{User-Agent} identifica al cliente como un navegador Chrome con IP de origen externa a la red institucional, lo que confirma que la solicitud atravesó el recorrido completo: reverse proxy perimetral, Traefik, contenedor Socat y servidor físico TrueNAS.

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{figuras/capitulo4/4.3/minio_logs_flujo_directo.png}
    \caption[Registro de acceso HTTP de MinIO: flujo directo desde el navegador]{Registro de acceso HTTP del servidor MinIO mostrando una operación \texttt{PUT 200 OK} originada desde un navegador Chrome. El campo \texttt{User-Agent} identifica el flujo directo documentado en la Sección~\ref{sec:proxy_socat}.}
    \label{fig:minio_log_flujo_directo}
\end{figure}

La Figura~\ref{fig:minio_log_flujo_indirecto} muestra el registro de una operación \texttt{GET} con código \texttt{200 OK} sobre ese bucket. En este caso, el campo \texttt{User-Agent} indica \texttt{node}, lo que identifica al cliente como el proceso Node.js del backend de Documenso. Este registro corresponde al flujo indirecto: la aplicación recupera el documento PDF desde MinIO a través del proxy Socat para procesarlo internamente.

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{figuras/capitulo4/4.3/minio_logs_flujo_indirecto.png}
    \caption[Registro de acceso HTTP de MinIO: flujo indirecto desde el backend]{Registro de acceso HTTP del servidor MinIO mostrando una operación \texttt{GET 200 OK} con \texttt{User-Agent: node}. El identificador confirma que el cliente es el proceso backend de Documenso, correspondiente al flujo indirecto.}
    \label{fig:minio_log_flujo_indirecto}
\end{figure}

\FloatBarrier

Ambos registros, observados desde el servidor de almacenamiento, confirman que los dos flujos definidos en el diseño operan correctamente en producción: el canal directo navegador-MinIO y el canal indirecto backend-MinIO reciben respuestas \texttt{200 OK} del servidor, lo que valida la cadena completa de conectividad a través del proxy Socat.

\subsection{Comunicación con Browserless (renderizado del certificado de firma)}
\label{subsec:validacion_browserless}

Los registros de los contenedores \texttt{documenso} y \texttt{browserless} se analizaron para verificar que el renderizado del certificado de firma opera sin errores en producción.

El Código~\ref{lst:documenso_seal_trigger} contiene el fragmento de los registros de Documenso al momento en que se disparó el job de sellado para el documento~12.

\begin{lstlisting}[caption={Registros del contenedor Documenso: disparo del job \texttt{internal.seal-document}}, label={lst:documenso_seal_trigger}]
2026-Apr-11 17:48:46
[JOBS]: Triggering job internal.seal-document with payload
{ documentId: 12, requestMetadata: { ipAddress: '148.222.222.66',
  userAgent: 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) ...' } }
2026-Apr-11 17:48:46
Submitting job to endpoint:
http://documenso:3000/api/jobs/internal.seal-document/cmnumpeza003ep32ci0w10zsx
\end{lstlisting}

El Código~\ref{lst:browserless_seal_success} registra la respuesta de Browserless, comprendida entre los instantes 17:48:52 y 17:49:11 UTC.

\begin{lstlisting}[caption={Registros del contenedor Browserless: recepción y completado del job de renderizado}, label={lst:browserless_seal_success}]
17:48:52 browserless:job    [...]: Inbound WebSocket request.
17:48:52 browserless:job    [...]: Adding new job to queue.
17:48:52 browserless:system Generating fresh chrome browser
17:48:52 browserless:chrome-helper Launching Chrome with args:
         { "executablePath": "/usr/bin/google-chrome" }
17:49:06 browserless:system Got chrome instance
17:49:11 browserless:server [...]: Recording successful stat and cleaning up.
17:49:11 browserless:chrome-helper Browser process [...] has closed, cleaning up.
\end{lstlisting}

\FloatBarrier

Los registros evidencian que Browserless recibió la solicitud de renderizado por WebSocket, lanzó una instancia de Chrome desde \texttt{/usr/bin/google-chrome} y completó el procesamiento en aproximadamente 18 segundos. El mensaje \texttt{Recording successful stat and cleaning up} confirma que el job concluyó sin errores. Este resultado confirma que la limitación documentada en la Figura~\ref{fig:browserless_logs_error} quedó resuelta en producción y que el sellado criptográfico del documento puede completarse.

\FloatBarrier

\subsection{Entregabilidad del correo transaccional (Brevo SMTP)}
\label{subsec:validacion_smtp}

La Figura~\ref{fig:smtp_auth_headers} muestra los encabezados de autenticación de un correo de invitación a firmar enviado por Documenso y recibido en Gmail. Los tres mecanismos —SPF, DKIM y DMARC— registran resultado \texttt{PASS} y el mensaje fue entregado directamente en la bandeja de entrada, sin ser clasificado como spam.

\begin{figure}[!htbp]
    \centering
    \colorbox{black}{\includegraphics[width=0.85\textwidth]{figuras/capitulo4/4.3/headers_mail.png}}
    \caption[Encabezados de autenticación SMTP de un correo transaccional de Documenso]{Encabezados de autenticación extraídos de un correo de invitación a firmar recibido en Gmail. Los resultados \texttt{PASS} en SPF, DKIM y DMARC confirman que el relay de Brevo emite correos autenticados a nombre del dominio institucional.}
    \label{fig:smtp_auth_headers}
\end{figure}

\FloatBarrier

Los resultados \texttt{PASS} en los tres mecanismos confirman que el relay de Brevo emite correos autenticados a nombre del dominio institucional y que los mensajes transaccionales de Documenso superan los filtros anti-spam de los clientes de correo externos.

\section{Validación de Acceso y Control de Seguridad}
\label{sec:validacion_acceso}

Esta sección verifica que el mecanismo de acceso a la plataforma opera según lo configurado en la Subsección~\ref{subsec:google_oauth}: solo cuentas pertenecientes al dominio institucional pueden autenticarse, y el registro libre de nuevos usuarios está deshabilitado.

\subsection{Restricción del inicio de sesión con Google OAuth 2.0}
\label{subsec:validacion_oauth}

Al intentar iniciar sesión con una cuenta externa al dominio institucional, Google interrumpió el flujo de OAuth antes de emitir el código de autorización: el servidor de autorización devolvió el código \texttt{Error 403: org\_internal} con el mensaje "Acceso bloqueado: SistemaFirmasFACET solo se puede usar dentro de su organización" (Figura~\ref{fig:oauth_error_gmail}). Este comportamiento es consecuencia directa de haber configurado el tipo de aplicación como \textit{Interna} en Google Cloud Console, tal como se describió en la Subsección~\ref{subsec:google_oauth}: la restricción opera en la capa de autorización de Google y no depende de ninguna validación adicional por parte de la aplicación.

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.92\textwidth]{figuras/capitulo4/4.4/oauth_flow_gmail.png}
    \caption[Error de acceso con cuenta externa en Google OAuth]{Pantalla de error devuelta por Google al intentar autenticarse con una cuenta \texttt{@gmail.com}. El código \texttt{org\_internal} confirma que la restricción opera en la capa de autorización de Google.}
    \label{fig:oauth_error_gmail}
\end{figure}

Con una cuenta \texttt{@herrera.unt.edu.ar}, Google presentó el modal de consentimiento y, una vez aceptado, el servidor emitió el código de autorización; el backend de Documenso completó el intercambio y el usuario accedió al panel de trabajo.

\FloatBarrier

\subsection{Deshabilitación del registro local}
\label{subsec:validacion_signup}

El registro de usuarios mediante credenciales locales fue deshabilitado a través de la variable de entorno \texttt{NEXT\_PUBLIC\_DISABLE\_SIGNUP=true}, cuyo efecto en la interfaz se documentó en la Subsección~\ref{subsec:google_oauth} (Figura~\ref{fig:google_oauth_disable_signup}).

Se realizó una solicitud GET directa a \nolinkurl{https://documentos.facet.unt.edu.ar/signup} para verificar que la restricción opera a nivel de servidor. La solicitud retornó \texttt{302 Found} con \texttt{Location: /signin} (Figura~\ref{fig:signup_redirect_302}): la redirección es forzada por el backend antes de renderizar cualquier contenido de registro, lo que confirma que la restricción opera en la lógica de enrutamiento del servidor y no se limita a ocultar el formulario en el frontend.

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.75\textwidth]{figuras/capitulo4/4.4/request_endpoint_signup_302http.png}
    \caption[Respuesta HTTP 302 al acceder a la ruta \texttt{/signup}]{Captura de las herramientas de desarrollo del navegador mostrando la respuesta \texttt{302 Found} con redirección a \texttt{/signin} al intentar acceder directamente a la ruta de registro.}
    \label{fig:signup_redirect_302}
\end{figure}

\FloatBarrier

\section{Validación del Flujo Funcional de Extremo a Extremo}
\label{sec:validacion_e2e}

Esta sección valida la capa de sistema definida en la Sección~\ref{sec:metodologia_validacion}: el flujo completo de firma documental se ejecutó en producción con dos firmantes. La validación se presenta en dos partes: el ciclo operativo de extremo a extremo y la verificación de la firma digital incrustada en el documento resultante.

\subsection{Ciclo completo de firma documental}
\label{subsec:ciclo_e2e}

El ensayo se ejecutó sobre el documento \texttt{ejemplo1.pdf} con dos firmantes: fabricioblasich@gmail.com, con dirección externa al dominio institucional, y calbaca@herrera.unt.edu.ar, con cuenta institucional. El destinatario externo accedió mediante el enlace recibido por correo y completó la acción sin requerir cuenta local en la plataforma.

La Figura~\ref{fig:e2e_preparacion} muestra las etapas de preparación del documento. Desde el panel principal se seleccionó \texttt{ejemplo1.pdf} con el botón de carga. En el Paso~1 del editor se registraron los dos destinatarios con sus correos y nombres. En el Paso~2 se posicionaron los campos de firma sobre el lienzo del PDF, uno por destinatario en zonas diferenciadas del documento.

\begin{figure}[!htbp]
    \centering
    \begin{subfigure}[b]{0.80\textwidth}
        \centering
        \colorbox{black}{\includegraphics[width=\textwidth]{figuras/capitulo4/4.5/carga_documento_board_principal.png}}
        \caption{Panel principal: botón de carga}
        \label{fig:e2e_panel_carga}
    \end{subfigure}
    \vspace{0.5em}
    \begin{subfigure}[b]{0.80\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figuras/capitulo4/4.5/seleccion_firmantes.png}
        \caption{Paso 1: registro de destinatarios}
        \label{fig:e2e_firmantes}
    \end{subfigure}
    \vspace{0.5em}
    \begin{subfigure}[b]{0.80\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figuras/capitulo4/4.5/configuracion_campos.png}
        \caption{Paso 2: campos de firma sobre el PDF}
        \label{fig:e2e_campos}
    \end{subfigure}
    \caption[Preparación del documento: carga, destinatarios y posicionamiento de campos]{Panel de carga del documento (a), registro de los dos destinatarios en el Paso~1 del editor (b) y posicionamiento gráfico de los campos de firma en el Paso~2 (c).}
    \label{fig:e2e_preparacion}
\end{figure}

\FloatBarrier

Una vez enviado el documento, la Figura~\ref{fig:e2e_firmante} registra el flujo desde la perspectiva del segundo firmante. El correo transaccional, emitido por "Firmas FACET", llegó a la bandeja de entrada con el asunto "DEEC te invitó a firmar un documento". Al acceder al enlace, la interfaz de Documenso presentó al firmante su nombre precargado, la firma del primer destinatario ya aplicada sobre el lienzo y el indicador "1 Campo Restante" con navegación automática al campo pendiente. Al completarse la firma del segundo destinatario, la plataforma registró el campo como cubierto y disparó el job de sellado criptográfico descrito en la Subsección~\ref{subsec:validacion_browserless}.

\begin{figure}[!htbp]
    \centering
    \begin{subfigure}[b]{0.60\textwidth}
        \centering
        \colorbox{black}{\includegraphics[width=\textwidth]{figuras/capitulo4/4.5/correo_invitacion_recibido.png}}
        \caption{Correo de invitación recibido en bandeja de entrada}
        \label{fig:e2e_correo_inv}
    \end{subfigure}
    \vspace{1.5em}
    \begin{subfigure}[b]{0.90\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figuras/capitulo4/4.5/interfaz_del_firmante.png}
        \caption{Interfaz de firma con el campo del segundo firmante pendiente}
        \label{fig:e2e_interfaz_firma}
    \end{subfigure}
    \caption[Experiencia del firmante: correo de invitación e interfaz de firma]{Correo de invitación emitido por "Firmas FACET" recibido en la bandeja de entrada (a) e interfaz de Documenso con la firma del primer destinatario aplicada y el campo pendiente del segundo (b).}
    \label{fig:e2e_firmante}
\end{figure}

\FloatBarrier

La Figura~\ref{fig:e2e_cierre} registra el cierre del ciclo. Al completarse ambas firmas, la plataforma emitió el correo de notificación con el PDF firmado adjunto y persistió el artefacto \texttt{\_signed.pdf} en el bucket \texttt{documenso-prod} de MinIO. Esta persistencia cerró el ciclo de integración validado en la Sección~\ref{sec:verificacion_comunicacion}: el documento sellado criptográficamente quedó almacenado en el bucket verificado en la Subsección~\ref{subsec:validacion_minio}.

\begin{figure}[t]
    \centering
    \begin{subfigure}[b]{0.60\textwidth}
        \centering
        \colorbox{black}{\includegraphics[width=\textwidth]{figuras/capitulo4/4.5/mail_documento_completado.png}}
        \caption{Correo de notificación de completado con PDF firmado adjunto}
        \label{fig:e2e_mail_comp}
    \end{subfigure}
    \vspace{0.5em}
    \begin{subfigure}[b]{0.90\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figuras/capitulo4/4.5/signedDocument_uploadToMinio.png}
        \caption{Bucket \texttt{documenso-prod}: artefacto \texttt{\_signed.pdf} persistido en MinIO}
        \label{fig:e2e_minio_signed}
    \end{subfigure}
    \caption[Cierre del ciclo de firma: notificación de completado y persistencia en MinIO]{Correo de notificación emitido al concluir el ciclo con el PDF firmado adjunto (a) y registro del artefacto \texttt{\_signed.pdf} en el bucket \texttt{documenso-prod} de MinIO (b).}
    \label{fig:e2e_cierre}
\end{figure}

\FloatBarrier

\subsection{Verificación de la firma digital en el documento}
\label{subsec:verificacion_firma_adobe}

El PDF descargado al concluir el ciclo documentado en la Subsección~\ref{subsec:ciclo_e2e} se analizó en Adobe Acrobat para confirmar que el sellado criptográfico realizado por Documenso es verificable desde un visor estándar.

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.88\textwidth]{figuras/capitulo4/4.5/panel_firmas_adobe.png}
    \caption[Panel de firmas en Adobe Acrobat sobre el documento sellado]{Panel de firmas de Adobe Acrobat. Se indica que no hubo modificaciones en el documento desde que se firmó y se identifican los detalles del firmante institucional.}
    \label{fig:adobe_panel_firmas}
\end{figure}

El panel de firmas (Figura~\ref{fig:adobe_panel_firmas}) indica que no hubo modificaciones en el documento desde que se firmó; la advertencia de validez desconocida responde a que el certificado es autofirmado y no pertenece a una cadena de confianza comercial, comportamiento previsto en el diseño y documentado en la Subsección~\ref{sec:mecanismo_tecnico}.

La Figura~\ref{fig:adobe_visor_cert} detalla el certificado utilizado: emisor y sujeto son \texttt{documentos.facet.unt.edu.ar} (FACET), con vigencia del 28/03/2026 al 25/03/2036, lo que confirma que corresponde al material criptográfico generado durante el despliegue (Apéndice~\ref{AppendixB:Crypto}).

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.62\textwidth]{figuras/capitulo4/4.5/visor_certificados_adobe.png}
    \caption[Detalle del certificado institucional FACET en el visor de Adobe Acrobat]{Visor de certificados de Adobe Acrobat. Emisor y sujeto son \texttt{documentos.facet.unt.edu.ar} (FACET); vigencia del 28/03/2026 al 25/03/2036.}
    \label{fig:adobe_visor_cert}
\end{figure}

\FloatBarrier

\section{Métricas de Rendimiento del Sistema}
\label{sec:metricas_rendimiento}

El consumo de recursos del LXC se monitoreó mediante Netdata, desplegado como servicio adicional dentro de Coolify. Las métricas se recopilaron a nivel de \textit{cgroup} para cada contenedor, lo que permitió distinguir el comportamiento individual de Documenso, Browserless, PostgreSQL y el proxy Socat dentro de los 8~vCPUs y 8~GiB de RAM asignados al LXC (Subsección~\ref{sec:infraestructura_base}).

\subsection{Consumo de recursos en operación normal (\textit{baseline})}
\label{subsec:baseline}

El baseline se registró el 13 de abril de 2026 entre las 21:44 y las 22:00 UTC bajo condiciones de navegación estándar y descarga de archivos, sin ejecución de ciclos de firma concurrentes.

\subsubsection*{Memoria RAM}

La Figura~\ref{fig:baseline_ram} registra el consumo de RAM de los cuatro contenedores del stack durante el período de observación. Las curvas se mantuvieron planas a lo largo del período, lo que descartó fugas de memoria en operación normal.

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{figuras/capitulo4/4.6/BloqueA/baseline_ram.png}
    \caption[Consumo de RAM en baseline: cuatro contenedores del stack]{Consumo de RAM (en MiB) de los contenedores del stack durante el período de observación en baseline. Documenso: \textasciitilde351~MiB; Browserless: \textasciitilde308~MiB; PostgreSQL: \textasciitilde76~MiB; proxy Socat: \textasciitilde2,2~MiB. Total del stack: \textasciitilde737~MiB sobre 8~GiB asignados al LXC.}
    \label{fig:baseline_ram}
\end{figure}

\FloatBarrier

El consumo total del stack en reposo fue de aproximadamente 737~MiB, equivalente al 9\,\% de la capacidad del LXC. Documenso concentró la mayor parte de ese consumo (351~MiB), seguido de Browserless (308~MiB), cuya instancia de Chrome en espera conservaba un volumen de memoria preasignado. PostgreSQL y Socat aportaron conjuntamente menos de 80~MiB.

\subsubsection*{CPU}

En condición de reposo, los cuatro contenedores registraron consumos de CPU estrictamente puntuales (Figura~\ref{fig:baseline_cpu}).

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{figuras/capitulo4/4.6/BloqueA/baseline_cpu.png}
    \caption[Consumo de CPU en condición de reposo: los cuatro contenedores]{Uso de CPU (\% del límite del \textit{cgroup}) durante el período de observación. Picos máximos: Documenso 7,8\,\%; Browserless 2,5\,\%; PostgreSQL 1,3\,\%; proxy Socat 0,5\,\%.}
    \label{fig:baseline_cpu}
\end{figure}

\FloatBarrier

Los picos de Documenso (máximo 7,8\,\%) correspondieron a solicitudes de renderizado de página y no a procesamiento sostenido. Entre los instantes de actividad, todos los contenedores retornaron a valores próximos a cero. La holgura disponible respecto a los 8~vCPUs del LXC resultó amplia bajo esta carga.

\subsection{Consumo de recursos bajo carga controlada}
\label{subsec:carga_controlada}

Se realizaron dos ensayos de carga sobre el stack en producción: el sellado de un documento único y una ráfaga de cinco documentos enviados en intervalos breves. En ambos casos las métricas se recopilaron con la misma configuración de Netdata descrita en la Subsección~\ref{subsec:baseline}.

\subsubsection*{Ensayo 1: sellado de un documento}

Las Figuras~\ref{fig:carga_1firma_cpu} y \ref{fig:carga_1firma_ram} registran el comportamiento del stack durante el sellado de un documento individual.

\begin{figure}[!htbp]
    \centering
    \begin{subfigure}[b]{0.95\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figuras/capitulo4/4.6/BloqueB/carga_controlada_1firma_cpu.png}
        \caption{CPU durante el sellado de un documento}
        \label{fig:carga_1firma_cpu}
    \end{subfigure}
    \vspace{1em}
    \begin{subfigure}[b]{0.95\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figuras/capitulo4/4.6/BloqueB/carga_controlada_1firma_ram.png}
        \caption{RAM durante el sellado de un documento}
        \label{fig:carga_1firma_ram}
    \end{subfigure}
    \caption[CPU y RAM durante el sellado de un documento (1 firma)]{Consumo de CPU y RAM del stack durante el sellado de un documento. La actividad se concentró en un único instante (22:11:32--22:11:34 UTC): Browserless alcanzó un pico máximo de 104,5\,\% de CPU y 707,7~MiB de RAM antes de liberar ambos recursos al concluir el job.}
    \label{fig:carga_1_firma}
\end{figure}

\FloatBarrier

En CPU, el evento se manifestó como un pico aislado de 2 segundos de duración: Browserless alcanzó un máximo de 104,5\,\% mientras Documenso no superó el 19\,\%. El resto del stack permaneció en valores de \textit{baseline}. En RAM, Browserless escaló abruptamente desde 310~MiB hasta 707,7~MiB (+397~MiB) para instanciar el proceso de Chrome encargado del renderizado. Al concluir el job, el proceso fue destruido y el sistema operativo recuperó la memoria; ambas curvas retornaron a sus valores de \textit{baseline} en el siguiente intervalo de muestreo. El impacto sobre Documenso (+30~MiB) y PostgreSQL (+13~MiB) resultó marginal.

\subsubsection*{Ensayo 2: ráfaga de cinco documentos}

Las Figuras~\ref{fig:carga_5firmas_cpu} y \ref{fig:carga_5firmas_ram} registran el comportamiento del stack durante la ráfaga concurrente.

\begin{figure}[!htbp]
    \centering
    \begin{subfigure}[b]{0.95\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figuras/capitulo4/4.6/BloqueB/cpu_bajo_carga_controlada_5firmas.png}
        \caption{CPU durante la ráfaga de cinco documentos}
        \label{fig:carga_5firmas_cpu}
    \end{subfigure}
    \vspace{1em}
    \begin{subfigure}[b]{0.95\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figuras/capitulo4/4.6/BloqueB/ram_bajo_carga_controlada_5firmas.png}
        \caption{RAM durante la ráfaga de cinco documentos}
        \label{fig:carga_5firmas_ram}
    \end{subfigure}
    \caption[CPU y RAM durante la ráfaga de cinco documentos concurrentes]{Consumo de CPU y RAM del stack durante la ráfaga de cinco documentos. La ventana de actividad sostenida se extendió entre las 17:27:40 y las 17:29:20 UTC. Browserless alcanzó un pico de CPU de 104,5\,\% y un pico de RAM de 1~331~MiB antes de liberar ambos recursos al completarse todos los jobs.}
    \label{fig:carga_5_firmas}
\end{figure}

\FloatBarrier

En este ensayo, el bloque de actividad se extendió entre las 17:27:40 y las 17:29:20 UTC, aproximadamente dos minutos: Browserless procesó los cinco jobs en forma secuencial, lo que explica la forma escalonada de la curva de RAM (Figura~\ref{fig:carga_5firmas_ram}), donde cada peldaño corresponde al lanzamiento de un proceso de Chrome adicional. El pico máximo de RAM de Browserless fue de 1~331~MiB. Al completarse el último job, la curva cayó a los 298~MiB de \textit{baseline} en un único intervalo de muestreo.

El pico de CPU de 104,5\,\% observado en Browserless corresponde a 1,04 núcleos virtuales sobre los 8~vCPUs del LXC (el 13\,\% de la capacidad total). Documenso registró picos de hasta 20\,\% durante el mismo intervalo, asociados al encolamiento y despacho de los jobs de sellado.

\subsubsection*{Tabla comparativa de rendimiento}

La Tabla~\ref{tab:metricas_rendimiento} consolida los valores registrados en ambas subsecciones frente a la capacidad auditada del LXC.

\begin{table}[!htbp]
    \centering
    \caption[Métricas de rendimiento: \textit{baseline} vs.\ pico bajo carga]{Comparativa de consumo de recursos en \textit{baseline} y bajo carga controlada (ráfaga de 5 documentos). El límite total del LXC es de 8~vCPUs y 8~192~MiB de RAM.}
    \label{tab:metricas_rendimiento}
    \begin{tabular}{lrrr}
        \toprule
        \textbf{Componente} & \textbf{\textit{Baseline}} & \textbf{Pico (ráfaga)} & \textbf{Impacto sobre LXC} \\
        \midrule
        \multicolumn{4}{l}{\textbf{Uso de CPU} (capacidad total: 8~vCPUs)} \\
        \quad Browserless   & 2,5\,\%    & 104,5\,\%              & \texttildelow 13\,\% (\texttildelow 1~vCPU) \\
        \quad Documenso     & 7,77\,\%   & \texttildelow 20\,\%   & \texttildelow 2,5\,\% (\texttildelow 0,2~vCPU) \\
        \midrule
        \multicolumn{4}{l}{\textbf{Uso de RAM} (capacidad total: 8~192~MiB)} \\
        \quad Browserless             & \texttildelow 308~MiB  & \texttildelow 1~331~MiB & \texttildelow 16\,\% \\
        \quad \textbf{Stack total}    & \textbf{\texttildelow 737~MiB (9\,\%)} & \textbf{\texttildelow 1~826~MiB (22\,\%)} & \textbf{\texttildelow 22\,\%} \\
        \bottomrule
    \end{tabular}
\end{table}

\FloatBarrier

El recurso con mayor variación bajo carga fue la RAM de Browserless, que se multiplicó por 4,3 respecto al \textit{baseline}. Aun en el peor caso registrado, el stack consumió el 22\,\% de la RAM del LXC, lo que dejó un margen de aproximadamente 6~366~MiB (\texttildelow 6,2~GiB) disponibles. La CPU no representó un cuello de botella en ninguno de los dos ensayos.

\section{Validación del Respaldo y Recuperación}
\label{sec:validacion_respaldo}

Esta sección verifica la capa de continuidad definida en la Sección~\ref{sec:metodologia_validacion}: los mecanismos de respaldo automático configurados en la Sección~\ref{sec:estrategia_respaldo} del Capítulo~\ref{chap:diseno_implementacion} generan y persisten los artefactos esperados en el almacenamiento externo. La validación se apoya en la inspección del bucket de MinIO y en los metadatos de los objetos almacenados; el procedimiento operativo de restauración se documenta en el Apéndice~\ref{AppendixC}.

\subsection{Respaldo de la base de datos}
\label{subsec:validacion_respaldo_bd}

El \textit{job} de respaldo de PostgreSQL fue configurado en Coolify para ejecutarse diariamente a las 00:00 y persistir los volcados en el bucket \texttt{documenso-backup-databases} del servidor MinIO. La Figura~\ref{fig:minio_bucket_bd_backups} muestra el contenido del bucket al 14 de abril de 2026.

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{figuras/capitulo4/4.7/bucket_documenso-backup-databases.png}
    \caption[Bucket \texttt{documenso-backup-databases}: listado de volcados diarios de PostgreSQL]{Consola de MinIO mostrando el bucket \texttt{documenso-backup-databases}. Se registran 18 objetos con nomenclatura \texttt{pg-dump-documensoprod-db-\{timestamp\}.dmp}, generados diariamente desde el 28 de marzo de 2026. El tamaño total de los artefactos es de 14,1~MiB.}
    \label{fig:minio_bucket_bd_backups}
\end{figure}

\FloatBarrier

El listado registra 18 volcados con una cadencia estrictamente diaria: la marca de tiempo UNIX incluida en cada nombre de archivo garantiza la unicidad del artefacto e impide colisiones de nombres ante reinicios del job. Los volcados se almacenan bajo la ruta \texttt{.../documensoprod-database-y4gssggkgc4s088kg48wks88}, donde el sufijo corresponde al identificador del contenedor de base de datos desplegado en el Capítulo~\ref{chap:diseno_implementacion}.

El tamaño de los artefactos refleja el crecimiento orgánico de la base de datos: los volcados del 2 al 10 de abril se mantuvieron en el rango de 742--744~KiB, período que coincide con el despliegue inicial sin carga de documentos. A partir del 11 de abril, coincidiendo con los ensayos de firma documentados en la Sección~\ref{sec:validacion_e2e}, el tamaño escaló progresivamente hasta 1,6~MiB al 14 de abril, lo que evidencia que cada ciclo de firma incorporó registros nuevos a la base de datos.

La Figura~\ref{fig:minio_metadata_bd_backup} muestra los metadatos del volcado del 7 de abril, inspeccionado como muestra representativa de la política de retención aplicada al bucket.

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.45\textwidth]{figuras/capitulo4/4.7/metadata_bucket_documenso-backup-databases.png}
    \caption[Metadatos del objeto \texttt{pg-dump-documensoprod-db-1775530804.dmp}: política de retención WORM]{Metadatos del volcado del 7 de abril en la consola de MinIO. El atributo \texttt{X-Amz-Object-Lock-Mode} registra el valor \texttt{GOVERNANCE} y el atributo \texttt{X-Amz-Object-Lock-Retain-Until-Date} fija el bloqueo hasta el 7 de mayo de 2026, lo que confirma una política de retención de 30 días.}
    \label{fig:minio_metadata_bd_backup}
\end{figure}

\FloatBarrier

Los metadatos confirman que el bucket opera con la política de bloqueo de objetos \textit{Object Lock} en modo \texttt{GOVERNANCE}, configurada con una retención de 30 días. Bajo este esquema, los artefactos son inmutables durante el período de retención: ningún proceso del LXC puede modificarlos ni eliminarlos una vez escritos, lo que protege los respaldos ante una eventual corrupción o compromiso del entorno de producción. Esta propiedad corresponde al modelo \textit{WORM} (\textit{Write Once, Read Many}) y constituye un control de seguridad adicional al almacenamiento externo documentado en la Sección~\ref{sec:estrategia_respaldo}.
\subsection{Prueba de restauración de la base de datos}
\label{subsec:validacion_restauracion_bd}

Se ejecutó un simulacro de restauración a partir de uno de los volcados almacenados en el bucket \texttt{documenso-backup-databases}. Coolify orquestó el comando \texttt{pg\_restore -{}-clean} sobre el contenedor de PostgreSQL, lo que forzó el reemplazo completo del estado previo de la base de datos. La integridad del resultado se verificó accediendo a la aplicación y confirmando que los registros de documentos y firmantes se encontraban íntegros. El procedimiento detallado para importar y restaurar los respaldos se documenta en el Apéndice~\ref{AppendixC:RestauracionBD}.

\subsection{Respaldo de la configuración del stack}
\label{subsec:validacion_respaldo_config}

El script \texttt{backup-coolify.sh}, configurado mediante \textit{cron} para ejecutarse diariamente a la 01:00, empaqueta los manifiestos y secretos del despliegue y transfiere el resultado al bucket \texttt{backups-coolify} de MinIO. La Figura~\ref{fig:minio_bucket_config_backups} muestra el contenido del bucket al 14 de abril de 2026.

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{figuras/capitulo4/4.7/respaldo_configuracion.png}
    \caption[Bucket \texttt{backups-coolify}: listado de respaldos diarios de configuración]{Consola de MinIO mostrando el bucket \texttt{backups-coolify}. Se registran 20 objetos con nomenclatura \texttt{coolify\_configs\_YYYY-MM-DD\_HHmmss.tar.gz}, generados diariamente desde el 28 de marzo de 2026. El tamaño total de los artefactos es de 552,3~KiB.}
    \label{fig:minio_bucket_config_backups}
\end{figure}

\FloatBarrier

Los 20 artefactos presentan un tamaño estable en el rango de 27,0--28,0~KiB, lo que indica que el contenido del stack no varió de forma significativa entre ejecuciones. La inspección del archivo del 7 de abril mediante \texttt{tar -{}-list} confirmó la presencia de los manifiestos y secretos del despliegue principal:

\begin{lstlisting}[caption={Inspección del contenido de \texttt{coolify\_configs\_2026-04-07\_040001.tar.gz}}, label={lst:tar_config_backup}]
$ tar -tvf coolify_configs_2026-04-07_040001.tar.gz
tar: Removing leading `/' from member names
[...]
-rw-r--r-- root/root  11596 2026-04-05 14:48
  /data/coolify/services/y4gssggkgc4s088kg48wks88/docker-compose.yml
-rw-r--r-- root/root   5925 2026-04-05 14:48
  /data/coolify/services/y4gssggkgc4s088kg48wks88/.env
[...]
\end{lstlisting}

El identificador \texttt{y4gssggkgc4s088kg48wks88} corresponde al stack de Documenso desplegado en el Capítulo~\ref{chap:diseno_implementacion}, que agrupa los contenedores de la aplicación, la base de datos y Browserless. La presencia del \texttt{docker-compose.yml} (11~596 bytes) y del archivo \texttt{.env} (5~925 bytes) confirma que el artefacto contiene la información suficiente para reconstruir el stack sin acceso al LXC.

La Figura~\ref{fig:minio_metadata_config_backup} muestra los metadatos del artefacto del 7 de abril.

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.45\textwidth]{figuras/capitulo4/4.7/metadata_respaldo_configuracion.png}
    \caption[Metadatos del objeto \texttt{coolify\_configs\_2026-04-07\_040001.tar.gz}: política de retención WORM]{Metadatos del respaldo de configuración del 7 de abril en la consola de MinIO. El atributo \texttt{X-Amz-Object-Lock-Mode} registra el valor \texttt{GOVERNANCE} y el atributo \texttt{X-Amz-Object-Lock-Retain-Until-Date} fija el bloqueo hasta el 4 de octubre de 2026, lo que confirma una política de retención de 180 días.}
    \label{fig:minio_metadata_config_backup}
\end{figure}

\FloatBarrier

Los metadatos confirman la aplicación de Object Lock en modo \texttt{GOVERNANCE} con una retención de 180 días, seis veces superior a la configurada para los volcados de base de datos. Bajo este esquema, los artefactos de configuración son inmutables durante el período de retención, lo que protege los manifiestos y secretos del despliegue ante una eventual corrupción o compromiso del entorno. El procedimiento de restauración del stack a partir de estos artefactos se documenta en el Apéndice~\ref{AppendixC:RestauracionConfig}.

\section{Trazabilidad y Cumplimiento de Requisitos}
\label{sec:trazabilidad}

La presente sección cierra el ciclo de validación del capítulo. La
Tabla~\ref{tab:trazabilidad} vincula cada requisito del alcance definido en el
Capítulo~\ref{chap:introduccion} con la sección del
Capítulo~\ref{chap:diseno_implementacion} donde fue abordado y con la evidencia
empírica que lo valida. A continuación, la Tabla~\ref{tab:comparativa_final} retoma
el análisis del estado del arte para contrastar las condiciones de las plataformas
comerciales con los resultados de la infraestructura desplegada.

\subsection{Matriz de trazabilidad}
\label{subsec:trazabilidad_requisitos}

\begin{table}[!htbp]
    \centering
    \small
    \caption[Matriz de trazabilidad de requisitos]{Trazabilidad entre los requisitos
    del Capítulo~\ref{chap:introduccion}, el diseño del
    Capítulo~\ref{chap:diseno_implementacion} y la validación del presente capítulo.
    Las columnas ``Diseño'' y ``Validación'' indican el número de la sección
    correspondiente.}
    \label{tab:trazabilidad}
    \begin{tabular}{p{3.8cm} p{1.6cm} p{1.6cm} p{5.2cm}}
        \toprule
        \textbf{Requisito} &
        \textbf{Diseño} &
        \textbf{Validación} &
        \textbf{Resultado} \\
        \midrule
        Selección de plataforma de código abierto &
        \ref{subsec:alternativas_opensource} &
        \ref{sec:validacion_e2e} &
        Documenso se desplegó y mantuvo operativo en producción; el flujo completo de
        firma se ejecutó con dos firmantes en condiciones reales. \\
        \addlinespace
        Despliegue contenerizado sobre infraestructura institucional (LXC, Coolify) &
        \ref{sec:infraestructura_base} &
        \ref{sec:validacion_despliegue} &
        Los cuatro servicios del stack alcanzaron el estado \texttt{Healthy} y se
        mantuvieron estables sobre el hardware disponible, incluida la restricción
        de procesadores sin soporte AVX. \\
        \addlinespace
        Autenticación restringida al dominio institucional &
        \ref{subsec:google_oauth} &
        \ref{sec:validacion_acceso} &
        Los intentos de autenticación con cuentas externas fueron bloqueados con
        código 403 antes de emitir el token; las cuentas
        \texttt{@herrera.unt.edu.ar} accedieron sin restricciones. \\
        \addlinespace
        Integración con almacenamiento de objetos (MinIO) mediante proxy Socat &
        \ref{sec:proxy_socat} &
        \ref{subsec:validacion_minio} &
        Las operaciones \texttt{PUT} y \texttt{GET} registraron código
        \texttt{200 OK} en los logs de MinIO; el documento sellado quedó persistido
        en el bucket \texttt{documenso-prod}. \\
        \addlinespace
        Correo transaccional con entregabilidad verificada (Brevo SMTP) &
        \ref{subsec:brevo_smtp} &
        \ref{subsec:validacion_smtp} &
        SPF, DKIM y DMARC registraron resultado \texttt{PASS}; los correos de
        invitación y notificación se entregaron en la bandeja de entrada sin ser
        clasificados como spam. \\
        \addlinespace
        Firma digital con certificado institucional &
        \ref{subsec:documenso_core} &
        \ref{subsec:verificacion_firma_adobe} &
        La firma incrustada en Adobe Acrobat indicó ausencia de modificaciones
        posteriores al sellado; la advertencia de cadena de confianza es
        consistente con el uso de certificado autofirmado, limitación declarada
        en el alcance. \\
        \addlinespace
        Estrategia de respaldo y retención de datos &
        \ref{sec:estrategia_respaldo_retencion} &
        \ref{sec:validacion_respaldo} &
        Se acumularon 18 volcados de base de datos (GOVERNANCE, 30~días) y
        20~respaldos de configuración (GOVERNANCE, 180~días) en TrueNAS/MinIO;
        las políticas de inmutabilidad se confirmaron mediante inspección de
        metadatos. \\
        \addlinespace
        Consumo de recursos dentro de la capacidad del LXC &
        \ref{sec:infraestructura_base} &
        \ref{sec:metricas_rendimiento} &
        El consumo máximo bajo carga de ráfaga fue del 22\,\% de la RAM del LXC,
        con un margen remanente de 6~366~MiB. \\
        \addlinespace
        Elaboración del manual de mantenimiento &
        \ref{sec:objetivos_alcance} &
        Apéndice~\ref{AppendixC:Manual} &
        Los procedimientos de restauración de base de datos y configuración
        quedaron documentados en el Apéndice~C, cumpliendo el objetivo de
        transferencia de conocimiento al área de TI. \\
        \bottomrule
    \end{tabular}
\end{table}

\FloatBarrier

\subsection{Comparativa con el estado del arte}
\label{subsec:comparativa_estado_arte}

La Tabla~\ref{tab:comparativa_final} retoma el análisis de mercado presentado en la
Sección~\ref{sec:estado_arte} del Capítulo~\ref{chap:introduccion} y lo contrasta
con los resultados de la solución implementada. El propósito no es establecer una
superioridad absoluta sobre los proveedores comerciales, sino verificar que la
alternativa de código abierto resolvió los condicionantes que motivaron su adopción
en el contexto institucional de la FACET.

\begin{table}[!htbp]
    \centering
    \small
    \caption[Comparativa final: plataformas SaaS versus solución implementada]{Contraste
    entre las condiciones de las plataformas comerciales relevadas en la
    Sección~\ref{sec:estado_arte} y los resultados de la infraestructura desplegada.}
    \label{tab:comparativa_final}
    \begin{tabular}{p{3.4cm} p{4.5cm} p{4.5cm}}
        \toprule
        \textbf{Aspecto} &
        \textbf{Plataformas SaaS} &
        \textbf{Solución implementada} \\
        \midrule
        Costo de licenciamiento (20~usuarios) &
        \texttildelow 5.326~USD anuales recurrentes &
        Costo nulo en licencias \\
        \addlinespace
        Soberanía de los datos &
        Documentos alojados en servidores externos al dominio institucional &
        Datos resguardados en TrueNAS/MinIO dentro de la infraestructura de la UNT \\
        \addlinespace
        Gestión de identidad &
        Delegada al proveedor; la integración con el directorio propio depende
        del plan contratado &
        Google OAuth~2.0 restringido al dominio \texttt{@herrera.unt.edu.ar},
        sin costo adicional \\
        \addlinespace
        Certificado de firma &
        Emitido y administrado por el proveedor &
        Certificado institucional generado y controlado por la FACET \\
        \addlinespace
        Carga operativa y disponibilidad &
        Garantizada contractualmente mediante SLA &
        Bajo responsabilidad del área TI de la facultad; sin SLA formal \\
        \bottomrule
    \end{tabular}
\end{table}

\FloatBarrier

La adopción del esquema on-premise implica que el área de TI de la facultad asume
la carga operativa que en los servicios comerciales queda cubierta por contrato.
Como contrapartida, la solución eliminó el costo recurrente de 5.326~USD anuales
proyectado en el estudio inicial, mantuvo los documentos dentro de la
infraestructura propia de la UNT y consolidó la integración con el directorio
institucional sin dependencia de un proveedor externo.
